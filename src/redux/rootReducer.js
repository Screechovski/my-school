export function rootReducer(state, action) {
    switch (action.type) {
        case "WRITE_MESSAGETITLE":
            
            break;
        case "WRITE_MESSAGE":
            
            break;
    
        default:
            break;
    }
    return state;
}